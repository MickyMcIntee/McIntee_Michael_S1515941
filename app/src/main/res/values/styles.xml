<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPastelBG</item>
        <item name="android:datePickerDialogTheme">@style/DialogTheme</item>
        <item name="android:itemTextAppearance">@style/Font</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="Font">
        <item name="android:textSize">22dp</item>
    </style>

    <style name="ContextFont">
        <item name="android:textSize">22dp</item>
        <item name="android:textColor">@color/colorPastelDarker</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Rule">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">2dp</item>
        <item name="android:background">#000000</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <style name="spinnerStyle" parent="android:TextAppearance.Widget.TextView.SpinnerItem">
        <item name="android:textSize">20dp</item>
    </style>

    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">@color/colorPastelDarker</item>
        <item name="colorPrimaryDark">@color/colorPastelBG</item>
        <item name="colorAccent">@color/colorPastelButton</item>
        <item name="android:calendarViewStyle">@style/calStyle</item>
    </style>
    <style name="calStyle" parent="@android:style/Widget.Material.CalendarView">
        <item name="android:dateTextAppearance">@style/dayStyle</item>
        <item name="android:weekDayTextAppearance">@style/dayStyle</item>
        <item name="android:background">@color/colorPastelDarker</item>
    </style>
    <style name="dayStyle" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:textSize">22dp</item>
        <item name="android:textColor">@color/colorPastelBG</item>
    </style>
</resources>
